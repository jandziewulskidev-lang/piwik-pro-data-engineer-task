[project]
name = "data-engineer-task"
version = "0.1.0"
description = "PiwikPRO data engineer task"
requires-python = ">=3.11"

dependencies = [
  "pandas",
  "openpyxl",
  "duckdb",
  "dbt-core",
  "dbt-duckdb",
]
